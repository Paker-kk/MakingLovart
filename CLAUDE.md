@AGENTS.md

## Claude Code

Use the Flovart MCP server or `npm run flovart:cli -- <task>` when the user asks to operate the active canvas from outside the app.

Prefer the project skill under `.claude/skills/flovart` when the task mentions Flovart, Canvas, Codex/Claude/OpenCode integration, MCP, runtime tools, or agent shell behavior.
